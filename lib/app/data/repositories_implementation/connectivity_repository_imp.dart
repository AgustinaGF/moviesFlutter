import 'package:movies_flutter/app/domain/repositories/connectivity_repository.dart';

class ConnectivityRepositoryImp implements ConnectivityRepository {
  @override
  Future<bool> get hasInternet => throw UnimplementedError();
}
